$kind: http-request
url: http://localhost:8080/api/auth/login
method: POST
body:
  type: json
  content: "{\r

    \  \"email\": \"test@example.com\",\r

    \  \"password\": \"SecurePassword123!\"\r

    }"
scripts:
  - type: afterResponse
    code: "if (pm.response.code === 200) {\r

      \    let responseData = pm.response.json();\r

      \    // Saves the token from the response into a collection variable\r

      \    pm.collectionVariables.set(\"jwt_token\", responseData.token);\r

      \    console.log(\"JWT automatically saved to variables!\");\r

      }"
    language: text/javascript
order: 2223031602751245
