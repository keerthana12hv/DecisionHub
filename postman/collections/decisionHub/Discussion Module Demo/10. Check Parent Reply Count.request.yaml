$kind: http-request
name: 10. Check Parent Reply Count
url: http://localhost:8080/api/decisions/{{decisionId}}/comments
method: GET
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
order: 10000
