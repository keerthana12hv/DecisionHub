$kind: http-request
name: Request 12 — Get Full Ranking
url: http://localhost:8080/api/decisions/2/ranking
method: GET
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
order: 4503552177342371
