$kind: http-request
name: publish decision
url: http://localhost:8080/api/decisions/2/publish
method: POST
body:
  type: json
  content: ""
auth:
  type: bearer
  credentials:
    token: "{{authToken}}"
order: 12000
